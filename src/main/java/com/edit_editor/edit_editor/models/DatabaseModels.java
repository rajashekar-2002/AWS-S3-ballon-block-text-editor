package com.edit_editor.edit_editor.models;

public class DatabaseModels {
    public enum Status {
        PUBLISH,
        DRAFT
    }
    
    public enum Category {
        CODE,
        MOVIE,
        CRAFT
    }
}
